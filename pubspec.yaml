name: pusher
description: Pusher Dart REST library
version: 0.0.1
environment:
  sdk: '>=1.0.0 <2.0.0'

dependencies:
  crypto: "^0.9.0"
  http: "^0.11.3+2"

dev_dependencies:
  test: '>=0.12.0 <0.13.0'